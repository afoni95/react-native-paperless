name: Build Dev APKs

on:
  workflow_dispatch:

permissions:
  contents: read
jobs:
  build-android:
    name: Build Android Dev APK
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: latest
          cache: npm

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Cache Gradle dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('android/**/*.gradle*', 'android/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-${{ runner.os }}-

      - name: Install dependencies
        run: npm ci

      - name: Run TypeScript check
        run: npx tsc --noEmit

      - name: Generate native project
        run: npx expo prebuild --platform android --clean

      - name: Write release keystore
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > android/app/upload-keystore.jks
          echo "storeFile=upload-keystore.jks"              >  android/key.properties
          echo "storePassword=${{ secrets.KEYSTORE_PASSWORD }}" >> android/key.properties
          echo "keyAlias=${{ secrets.KEY_ALIAS }}"          >> android/key.properties
          echo "keyPassword=${{ secrets.KEY_PASSWORD }}"    >> android/key.properties

      # Enable ABI splits (arm64-v8a + armeabi-v7a)
      - name: Enable ABI splits
        run: |
          echo '
          android {
              splits {
                  abi {
                      enable true
                      reset()
                      include "arm64-v8a", "armeabi-v7a"
                      universalApk false
                  }
              }
          }
          ' >> android/app/build.gradle

      - name: Build Release APKs
        working-directory: android
        run: ./gradlew assembleRelease --no-daemon --stacktrace

      - name: Upload modern-devices APK (arm64-v8a)
        uses: actions/upload-artifact@v4
        with:
          name: android-modern-devices-apk
          path: android/app/build/outputs/apk/release/*arm64-v8a*.apk

      - name: Upload legacy-devices APK (armeabi-v7a)
        uses: actions/upload-artifact@v4
        with:
          name: android-legacy-devices-apk
          path: android/app/build/outputs/apk/release/*armeabi-v7a*.apk